#ifndef ATHENA_DRAW_H
#define ATHENA_DRAW_H

/*
 * 2D primitives. The implementation lives in the graphics module
 * (draw_point, draw_line, draw_triangle, draw_triangle_gouraud, draw_quad,
 * draw_quad_gouraud, draw_sprite, draw_circle); this header is the entry
 * point for C applications that select the draw module.
 */

#include <athena/graphics.h>
#include <athena/color.h>

#endif /* ATHENA_DRAW_H */
